{
  "name": "db-agent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install --include=dev && npm run build"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
