# Anthropic API key (required for NL query generation)
ANTHROPIC_API_KEY=sk-ant-...

# Where agent configs are stored on disk
AGENTS_DIR=./agents

# Server settings
HOST=0.0.0.0
PORT=8000
RELOAD=true

# Max rows returned per query (safety cap)
MAX_QUERY_ROWS=500
